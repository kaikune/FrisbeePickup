<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{title}}</title>
	    <link rel="stylesheet" href="/public/css/styles.css">
    </head>
    <body>
        <header>
            <div id="header-menu">
                <a href="/"><div class="header-left" >Home</div></a>
                <a href="/search"><div class="header-center" >Search</div></a>
                <a href="/create-group"><div class="header-center" >Create group</div></a>
                <a href="/create-game"><div class="header-center" >Create Event</div></a>
                    <div class="header-right dropdown" id="header-profile-box"> 
                        {{#if currentUser}}
                        <a>{{ currentUser.username }}</a>
                        <div class="dropdown-content">
                            <a href="/users/{{currentUser._id}}">Profile</a>
                            <a href="/logout">Logout</a>
                        </div>
                        {{else}}
                        <a>Authentication</a>
                        <div class="dropdown-content">
                            <a href="/login">Login</a>
                            <a href="/register">Register</a>
                        </div>
                        {{/if}}
                    </div>
            </div>
        </header>
        <div id="body-background">
        {{{ body }}}
        </div>
        <footer>
            <a>This website was created for RIC3 Family Frisbee</a>
        </footer>
        <script src='/public/js/main.js'></script>
    </body>
</html>